# WARNING: This is a template. Replace placeholder values before applying.
# For production, use a secrets manager (e.g., Sealed Secrets, External Secrets, Vault).
#
# To encode values: echo -n 'your-value' | base64
apiVersion: v1
kind: Secret
metadata:
  name: vocaltext-secrets
  namespace: voooice
  labels:
    app.kubernetes.io/part-of: vocaltext
type: Opaque
data:
  JWT_SECRET: REPLACE_WITH_BASE64_ENCODED_VALUE
  ELEVENLABS_API_KEY: REPLACE_WITH_BASE64_ENCODED_VALUE
  STRIPE_WEBHOOK_SECRET: REPLACE_WITH_BASE64_ENCODED_VALUE
